apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llm-monitoring-dashboard-data
  namespace: ai-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
