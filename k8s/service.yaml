apiVersion: v1
kind: Service
metadata:
  name: llm-monitoring-dashboard
  namespace: ai-platform
  labels:
    app.kubernetes.io/name: llm-monitoring-dashboard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: llm-monitoring-dashboard
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
